app:
  jwt:
    secret: test-secret-key-with-at-least-256-bits-for-hmac-sha256!!
    expiration-ms: 60000
  cors:
    allowed-origins: http://localhost:5173

spring:
  jpa:
    hibernate:
      ddl-auto: validate
